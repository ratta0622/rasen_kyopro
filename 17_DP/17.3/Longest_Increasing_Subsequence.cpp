#include <bits/stdc++.h>
using namespace std;
using P = pair<int,int>;

int main(){
  int N;
  cin >> N;
  int a[N+1];
  a[0] = INT_MAX;
  for(int i=1; i<N; ++i){
    cin >> a[i];
  }

  dp[0]={};

  for(int i=1; i<N; ++i){
  }
  
 
  return 0;
}
